services:
  valkey:
    image: valkey/valkey:9-alpine
    restart: unless-stopped
    ports:
      - 5007:6379
    volumes:
      - ./data:/data
    command: >
      valkey-server --save 60 1 --loglevel warning --disable-thp yes
      --requirepass ${VALKEY_PASSWORD}